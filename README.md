# Earnings Intelligence Agent

An agentic AI system that autonomously analyzes SEC filings and market data to generate institutional-grade investment research briefs — powered by Claude AI, FastAPI, and scikit-learn.

---

## Overview

The Earnings Intelligence Agent takes a stock ticker as input and autonomously:

1. Fetches the latest 10-K or 10-Q filing directly from the SEC EDGAR API
2. Pulls live market data, price history, and earnings metrics via Yahoo Finance
3. Runs NLP sentiment analysis on filing language using a scikit-learn TF-IDF classifier
4. Orchestrates a multi-step Claude AI agent to reason across all data sources and generate a structured investment research brief

The result is a Goldman Sachs-style research output — company overview, earnings analysis, sentiment assessment, bull case, bear case, key risks, and investment outlook with a price target — delivered in seconds via a production REST API.

---

## Sample Output

The following is real output generated by the system for Apple Inc. (AAPL) using the fiscal year 2025 10-K filing:

```json
{
    "ticker": "AAPL",
    "company_name": "Apple Inc.",
    "filing": {
        "form_type": "10-K",
        "filed_date": "2025-10-31",
        "accession_number": "0000320193-25-000079",
        "document_url": "https://www.sec.gov/Archives/edgar/data/320193/000032019325000079/aapl-20250927.htm"
    },
    "sentiment": {
        "score": -0.1522,
        "label": "negative",
        "confidence": 0.5958
    },
    "brief": {
        "company_overview": "Apple Inc. (NASDAQ: AAPL) remains the world's pre-eminent consumer electronics franchise, with a fiscal 2025 revenue base of $451.4B and net income of $122.6B...",
        "earnings_analysis": "Trailing-twelve-month metrics show EPS of $8.27 on revenue of $451.4B, yielding a net margin of approximately 27.2%...",
        "sentiment_assessment": "The quantitative sentiment model returns a score of -0.1522 (negative label) with moderate confidence of 0.5958, derived from the 10-K filing text...",
        "bull_case": "The structural Services flywheel remains Apple's most compelling driver. With a two-billion-plus device installed base...",
        "bear_case": "Valuation is the central bear argument. At 38.1x trailing and 32.8x forward earnings, the stock trades at a premium...",
        "key_risks": "Tariff and trade policy, Greater China exposure, valuation de-rating, iPhone cycle dependency, regulatory pressure on Services...",
        "investment_outlook": "We initiate at NEUTRAL with a 12-month price target of $311, in line with consensus. The quality of the franchise justifies holding, but the combination of a full valuation, negative implied upside, and disclosed macro/tariff risk argues against adding at current levels.",
        "model_used": "claude-opus-4-8",
        "generated_at": "2026-06-02T20:35:06.266307Z"
    }
}
```

---

## Architecture

```
earnings-intelligence-agent/
├── app/
│   ├── analysis/
│   │   ├── sentiment.py      # TF-IDF + Logistic Regression sentiment classifier
│   │   └── brief.py          # Claude API agentic research brief generator
│   ├── api/
│   │   └── routes.py         # FastAPI endpoint definitions
│   ├── fetchers/
│   │   ├── sec_edgar.py      # SEC EDGAR API client
│   │   └── yahoo_finance.py  # yfinance market data client
│   ├── models/
│   │   └── schemas.py        # Pydantic request/response models
│   ├── config.py             # Environment and settings management
│   └── main.py               # FastAPI app entry point
├── tests/                    # pytest test suite
├── scripts/
│   └── smoke_test.py         # End-to-end smoke test
├── .env.example              # Environment variable template
└── requirements.txt
```

**Pipeline flow:**
```
Ticker Input
    → SEC EDGAR API (10-K / 10-Q filing text)
    → Yahoo Finance (price, earnings, market cap)
    → Sentiment Analyzer (TF-IDF + scikit-learn)
    → Claude AI Agent (multi-step reasoning across all data)
    → Structured Research Brief (JSON via FastAPI)
```

---

## Tech Stack

| Layer | Technology |
|---|---|
| API Framework | FastAPI |
| AI Agent | Anthropic Claude API (claude-opus-4-8) |
| ML / NLP | scikit-learn, TF-IDF, Logistic Regression |
| Financial Data | SEC EDGAR API, yfinance |
| Data Validation | Pydantic v2 |
| HTTP Client | httpx |
| Testing | pytest |
| Language | Python 3.11+ |

---

## API Endpoints

### `POST /api/v1/analyze/{ticker}`

Runs the full agentic pipeline for a given stock ticker.

**Parameters:**
- `ticker` (path) — Stock ticker symbol e.g. `AAPL`, `MSFT`, `NVDA`
- `form_type` (query, optional) — SEC filing type: `10-K` (annual) or `10-Q` (quarterly). Default: `10-K`

**Example:**
```bash
curl -s -X POST "http://localhost:8000/api/v1/analyze/AAPL?form_type=10-K" | python -m json.tool
```

**Response fields:**
- `ticker` — Stock symbol
- `company_name` — Full legal name from SEC
- `filing` — Filing metadata (type, date, accession number, URL)
- `sentiment` — Score (-1 to 1), label, confidence
- `brief` — Full research brief with all sections and price target

---

## Setup & Installation

### Prerequisites
- Python 3.11+
- Anthropic API key (get one at [console.anthropic.com](https://console.anthropic.com))

### Installation

```bash
# Clone the repository
git clone https://github.com/yourusername/earnings-intelligence-agent.git
cd earnings-intelligence-agent

# Install dependencies
pip install -r requirements.txt

# Set up environment variables
cp .env.example .env
# Edit .env and add your ANTHROPIC_API_KEY
```

### Running the API

```bash
uvicorn app.main:app --reload
```

The API will be available at `http://localhost:8000`

### Running Tests

```bash
pytest tests/
```

---

## Design Decisions

**Why FastAPI over Flask?**
FastAPI provides automatic OpenAPI documentation, native Pydantic integration for request/response validation, and async support — making it the right choice for a production API that may need to handle concurrent requests.

**Why a custom scikit-learn sentiment model over a pre-trained LLM for sentiment?**
SEC filing language is highly domain-specific. A lightweight TF-IDF + logistic regression model trained on finance-specific vocabulary runs in milliseconds, provides an interpretable confidence score, and does not require an additional API call — keeping the pipeline fast and cost-efficient. The Claude agent layer handles the deeper reasoning.

**Why structure the Claude prompt to return Pydantic-validated sections?**
Structured output ensures the API response is always consistent and machine-readable regardless of how Claude phrases its analysis — critical for any downstream system that consumes this API.

**Agentic design principle:**
The Claude agent is given the raw filing text, market data, and sentiment score simultaneously and instructed to reason across all three sources — not just summarize each in isolation. This multi-source synthesis is what produces the tension analysis (e.g. flagging when bullish price action contradicts cautious filing language) that makes the brief genuinely useful.

---

## Limitations & Future Work

- Sentiment model is trained on a curated finance word list; a fine-tuned transformer (FinBERT) would improve accuracy
- Currently supports US equities with SEC filings only
- Brief generation cost scales with filing length; chunking strategy could reduce token usage for very large filings
- A Streamlit frontend for non-technical users is planned

---

## Author

Sagarika Roychoudhury  
[linkedin.com/in/sagarikaroychoudhury](https://linkedin.com/in/sagarikaroychoudhury)
